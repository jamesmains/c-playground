#ifndef WORD_H
#define WORD_H
typedef struct Word {
    char text[30];
    // May want to add modifiers that make it so the more difficult the word, the more points it's worth or attempts given
} Word;
#endif